# domain

Cœur hexagonal, zéro dépendance externe.

- `model/` — value objects et entités (Document, Embedding, Query)
- `port/` — interfaces d'entrée/sortie (ABCs)
- `service/` — orchestration métier pure
